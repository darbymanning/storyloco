import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	variants,
} from './button.svelte'

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	variants as buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
}
